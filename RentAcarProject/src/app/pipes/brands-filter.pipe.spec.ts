import { BrandsFilterPipe } from './brands-filter.pipe';

describe('BrandsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BrandsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
